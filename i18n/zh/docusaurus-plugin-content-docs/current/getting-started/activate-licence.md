---
sidebar_position: 3
title: 激活授权
---

# 激活授权

授权字符串把固件镜像锁到一块具体的板子上。它由烧录工具生成并返回。

## 步骤

1. 烧录完成后，烧录工具会显示一长串授权字符串，**复制**它。
2. 如果板子没自动重启，手动重新上电。
3. 连上开放热点 `nmap-2.4g`，等 captive portal 弹出（或访问 `http://192.168.4.1`）。
4. 把授权字符串粘贴到 **Licence** 框，点按钮。
5. 成功后页面会显示确认信息，矿机重启进入正常模式。